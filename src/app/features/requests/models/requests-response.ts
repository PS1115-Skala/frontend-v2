export interface RequestsResponse {
  id: string;
  requester_id: string;
  room_id: string;
  subject_id: string;
  send_time: string;
  trimester_id: string;
  reason: string;
  material_needed: string;
  quantity: number;
  status: string;
}
